-- [[ VTZ HUB - FULL INTEGRATED VERSION ]]

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ==========================================
-- 1. ENGINE DA INTERFACE (BIBLIOTECA ORIGINAL)
-- ==========================================
local HubLib = {}
HubLib.MainColor = Color3.fromRGB(20, 20, 20)
HubLib.SidebarColor = Color3.fromRGB(15, 15, 15)
HubLib.AccentColor = Color3.fromRGB(81, 74, 255) 
HubLib.ToggleOffColor = Color3.fromRGB(45, 45, 45)
HubLib.TextColor = Color3.fromRGB(255, 255, 255)

local function makeDraggable(frame, parentFrame)
    local dragging, dragInput, mousePos, framePos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            mousePos = input.Position
            framePos = parentFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - mousePos
            parentFrame.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
        end
    end)
end

function HubLib:Init(hubTitle, subTitle)
    local player = Players.LocalPlayer
    local targetParent = nil
    pcall(function() targetParent = CoreGui end)
    if not targetParent then targetParent = player:WaitForChild("PlayerGui") end

    if targetParent:FindFirstChild("VxezeHub") then targetParent.VxezeHub:Destroy() end

    local ScreenGui = Instance.new("ScreenGui", targetParent)
    ScreenGui.Name = "VxezeHub"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.IgnoreGuiInset = true

    local ToggleBtn = Instance.new("TextButton", ScreenGui)
    ToggleBtn.Name = "VxezeToggle"
    ToggleBtn.Size = UDim2.new(0, 45, 0, 45)
    ToggleBtn.Position = UDim2.new(0, 10, 0.5, -22)
    ToggleBtn.BackgroundColor3 = self.AccentColor
    ToggleBtn.Text = "VX"
    ToggleBtn.TextColor3 = Color3.new(1,1,1)
    ToggleBtn.Font = Enum.Font.GothamBold
    ToggleBtn.TextSize = 14
    ToggleBtn.ZIndex = 500
    Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 10)
    makeDraggable(ToggleBtn, ToggleBtn)

    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 480, 0, 320)
    MainFrame.Position = UDim2.new(0.5, -240, 0.5, -160)
    MainFrame.BackgroundColor3 = self.MainColor
    MainFrame.BorderSizePixel = 0
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

    ToggleBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = not MainFrame.Visible
    end)

    local TopBar = Instance.new("Frame", MainFrame)
    TopBar.Size = UDim2.new(1, 0, 0, 35)
    TopBar.BackgroundTransparency = 1
    makeDraggable(TopBar, MainFrame)

    local Title = Instance.new("TextLabel", TopBar)
    Title.Size = UDim2.new(1, -20, 1, 0)
    Title.Position = UDim2.new(0, 15, 0, 0)
    Title.Text = string.upper(hubTitle) .. " | " .. subTitle
    Title.TextColor3 = self.TextColor
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 13
    Title.BackgroundTransparency = 1
    Title.TextXAlignment = Enum.TextXAlignment.Left

    local Sidebar = Instance.new("Frame", MainFrame)
    Sidebar.Size = UDim2.new(0, 120, 1, -35)
    Sidebar.Position = UDim2.new(0, 0, 0, 35)
    Sidebar.BackgroundColor3 = self.SidebarColor
    Sidebar.BorderSizePixel = 0
    Instance.new("UIListLayout", Sidebar).HorizontalAlignment = Enum.HorizontalAlignment.Center
    Instance.new("UIPadding", Sidebar).PaddingTop = UDim.new(0, 10)

    self.PagesFolder = Instance.new("Frame", MainFrame)
    self.PagesFolder.Size = UDim2.new(1, -130, 1, -45)
    self.PagesFolder.Position = UDim2.new(0, 125, 0, 40)
    self.PagesFolder.BackgroundTransparency = 1

    self.Sidebar = Sidebar
    return self
end

function HubLib:CreateTab(name)
    local TabBtn = Instance.new("TextButton", self.Sidebar)
    TabBtn.Size = UDim2.new(0.9, 0, 0, 30)
    TabBtn.BackgroundTransparency = 1
    TabBtn.Text = name:upper()
    TabBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
    TabBtn.Font = Enum.Font.GothamSemibold
    TabBtn.TextSize = 11

    local Page = Instance.new("ScrollingFrame", self.PagesFolder)
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 0
    Page.CanvasSize = UDim2.new(0, 0, 0, 0)
    
    local layout = Instance.new("UIListLayout", Page)
    layout.Padding = UDim.new(0, 6)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    
    layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        Page.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 10)
    end)

    TabBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(self.PagesFolder:GetChildren()) do p.Visible = false end
        for _, b in pairs(self.Sidebar:GetChildren()) do if b:IsA("TextButton") then b.TextColor3 = Color3.fromRGB(150, 150, 150) end end
        Page.Visible = true
        TabBtn.TextColor3 = self.AccentColor
    end)

    if #self.Sidebar:GetChildren() <= 2 then Page.Visible = true TabBtn.TextColor3 = self.AccentColor end
    return Page
end

function HubLib:AddToggle(page, text, callback)
    local ToggleFrame = Instance.new("Frame", page)
    ToggleFrame.Size = UDim2.new(0.95, 0, 0, 38)
    ToggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", ToggleFrame).CornerRadius = UDim.new(0, 5)

    local Label = Instance.new("TextLabel", ToggleFrame)
    Label.Size = UDim2.new(1, -60, 1, 0)
    Label.Position = UDim2.new(0, 12, 0, 0)
    Label.Text = text
    Label.TextColor3 = self.TextColor
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 12
    Label.TextXAlignment = Enum.TextXAlignment.Left

    local Outer = Instance.new("TextButton", ToggleFrame)
    Outer.Size = UDim2.new(0, 38, 0, 18)
    Outer.Position = UDim2.new(1, -48, 0.5, -9)
    Outer.BackgroundColor3 = self.ToggleOffColor
    Outer.Text = ""
    Instance.new("UICorner", Outer).CornerRadius = UDim.new(1, 0)

    local Inner = Instance.new("Frame", Outer)
    Inner.Size = UDim2.new(0, 14, 0, 14)
    Inner.Position = UDim2.new(0, 2, 0, 2)
    Inner.BackgroundColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", Inner).CornerRadius = UDim.new(1, 0)

    local state = false
    Outer.MouseButton1Click:Connect(function()
        state = not state
        pcall(callback, state)
        local targetPos = state and UDim2.new(1, -16, 0, 2) or UDim2.new(0, 2, 0, 2)
        local targetColor = state and self.AccentColor or self.ToggleOffColor
        TweenService:Create(Outer, TweenInfo.new(0.2), {BackgroundColor3 = targetColor}):Play()
        TweenService:Create(Inner, TweenInfo.new(0.2), {Position = targetPos}):Play()
    end)
end

function HubLib:AddButton(page, text, callback)
    local Btn = Instance.new("TextButton", page)
    Btn.Size = UDim2.new(0.95, 0, 0, 38)
    Btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Btn.Text = text
    Btn.TextColor3 = self.TextColor
    Btn.Font = Enum.Font.GothamSemibold
    Btn.TextSize = 12
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 5)
    Btn.MouseButton1Click:Connect(callback)
end

function HubLib:AddTextBox(page, text, placeholder, callback)
    local TFrame = Instance.new("Frame", page)
    TFrame.Size = UDim2.new(0.95, 0, 0, 45)
    TFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", TFrame)
    local Box = Instance.new("TextBox", TFrame)
    Box.Size = UDim2.new(1, -24, 0, 20)
    Box.Position = UDim2.new(0, 12, 0, 18)
    Box.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Box.PlaceholderText = placeholder
    Box.Text = ""
    Box.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", Box)
    Box.FocusLost:Connect(function() callback(Box.Text) end)
end

function HubLib:AddDropdown(page, text, options, callback)
    local DropFrame = Instance.new("Frame", page)
    DropFrame.Size = UDim2.new(0.95, 0, 0, 38)
    DropFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", DropFrame)
    local Btn = Instance.new("TextButton", DropFrame)
    Btn.Size = UDim2.new(0, 120, 0, 28)
    Btn.Position = UDim2.new(1, -125, 0, 5)
    Btn.Text = "Selecionar..."
    Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    Btn.TextColor3 = self.AccentColor
    Instance.new("UICorner", Btn)
    local List = Instance.new("Frame", page)
    List.Size = UDim2.new(0.95, 0, 0, 0)
    List.Visible = false
    List.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Instance.new("UIListLayout", List)
    Instance.new("UICorner", List)
    Btn.MouseButton1Click:Connect(function()
        List.Visible = not List.Visible
        if List.Visible then
            for _, v in pairs(List:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            for _, opt in pairs(options) do
                local oBtn = Instance.new("TextButton", List)
                oBtn.Size = UDim2.new(1, 0, 0, 30)
                oBtn.Text = opt
                oBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                oBtn.TextColor3 = Color3.new(1,1,1)
                oBtn.MouseButton1Click:Connect(function()
                    Btn.Text = opt
                    callback(opt)
                    List.Visible = false
                end)
            end
            List.Size = UDim2.new(0.95, 0, 0, #List:GetChildren() * 30)
        end
    end)
end

function HubLib:AddEquipDropdown(page)
    local DropFrame = Instance.new("Frame", page)
    DropFrame.Size = UDim2.new(0.95, 0, 0, 38)
    DropFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", DropFrame).CornerRadius = UDim.new(0, 5)
    
    local Label = Instance.new("TextLabel", DropFrame)
    Label.Size = UDim2.new(1, -40, 1, 0)
    Label.Position = UDim2.new(0, 12, 0, 0)
    Label.Text = "AUTO EQUIP (SÓ ITENS)"
    Label.TextColor3 = self.TextColor
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 11
    Label.TextXAlignment = Enum.TextXAlignment.Left

    local Btn = Instance.new("TextButton", DropFrame)
    Btn.Size = UDim2.new(0, 30, 0, 30)
    Btn.Position = UDim2.new(1, -35, 0, 4)
    Btn.Text = "▼"
    Btn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Btn.TextColor3 = self.TextColor
    Btn.BorderSizePixel = 0
    Instance.new("UICorner", Btn)

    local List = Instance.new("Frame", page)
    List.Size = UDim2.new(0.95, 0, 0, 0)
    List.Visible = false
    List.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Instance.new("UIListLayout", List)
    Instance.new("UICorner", List)

    Btn.MouseButton1Click:Connect(function()
        List.Visible = not List.Visible
        if List.Visible then
            for _, v in pairs(List:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            for _, item in pairs(Players.LocalPlayer.Backpack:GetChildren()) do
                if not item.Name:lower():find("fruit") then
                    local iBtn = Instance.new("TextButton", List)
                    iBtn.Size = UDim2.new(1, 0, 0, 30)
                    iBtn.Text = item.Name
                    iBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
                    iBtn.TextColor3 = Color3.new(1,1,1)
                    iBtn.Font = Enum.Font.Gotham
                    iBtn.TextSize = 11
                    iBtn.MouseButton1Click:Connect(function()
                        Players.LocalPlayer.Character.Humanoid:EquipTool(item)
                        List.Visible = false
                    end)
                end
            end
            List.Size = UDim2.new(0.95, 0, 0, #List:GetChildren() * 30)
        end
    end)
end

-- ==========================================
-- 3. INICIALIZAÇÃO E ABAS DO VTZ HUB
-- ==========================================
local MyHub = HubLib:Init("KING LEGACY", "VTZ HUB")

-- ABA 1: FARMING (SUAS FUNÇÕES ORIGINAIS + NOVAS)
local FarmTab = MyHub:CreateTab("Farming")

MyHub:AddToggle(FarmTab, "Auto Farm Intelligence (Leveling)", function(state)
    _G.AutoFarmIntell = state
    if state then
        -- loadstring(game:HttpGet("SEU_LINK_AQUI"))()
    end
end)

MyHub:AddToggle(FarmTab, "Auto Collect Fruit", function(state)
    _G.FruitCollectorEnabled = state
    if state then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/tiktokbot116-commits/Scripts/refs/heads/main/AutoFruitKingLegacy"))()
        task.spawn(function() pcall(_G.StartFruitLoop) end)
    end
end)

MyHub:AddToggle(FarmTab, "Auto Collect All Chests", function(state)
    _G.AutoCollectAll = state
    if state then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/tiktokbot116-commits/Scripts/refs/heads/main/Auto%20Farm%20Chest"))()
    end
end)

-- MONITORES ORIGINAIS (FRUTAS E EVENTOS)
local Monitor = Instance.new("Frame", FarmTab)
Monitor.Size = UDim2.new(0.95, 0, 0, 70)
Monitor.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Instance.new("UICorner", Monitor).CornerRadius = UDim.new(0, 6)

local MTitle = Instance.new("TextLabel", Monitor)
MTitle.Size = UDim2.new(1, 0, 0, 25)
MTitle.Position = UDim2.new(0, 12, 0, 5)
MTitle.Text = "MONITORAMENTO DE FRUTAS"
MTitle.TextColor3 = HubLib.AccentColor
MTitle.Font = Enum.Font.GothamBold
MTitle.TextSize = 10
MTitle.BackgroundTransparency = 1
MTitle.TextXAlignment = Enum.TextXAlignment.Left

local FruitList = Instance.new("TextLabel", Monitor)
FruitList.Size = UDim2.new(1, -24, 1, -30)
FruitList.Position = UDim2.new(0, 12, 0, 30)
FruitList.Text = "SCANNING..."
FruitList.TextColor3 = Color3.fromRGB(150, 150, 150)
FruitList.Font = Enum.Font.GothamSemibold
FruitList.TextSize = 10
FruitList.BackgroundTransparency = 1
FruitList.TextYAlignment = Enum.TextYAlignment.Top
FruitList.TextXAlignment = Enum.TextXAlignment.Left

local EventFrame = Instance.new("Frame", FarmTab)
EventFrame.Size = UDim2.new(0.95, 0, 0, 60)
EventFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Instance.new("UICorner", EventFrame).CornerRadius = UDim.new(0, 6)

local EventTitle = Instance.new("TextLabel", EventFrame)
EventTitle.Size = UDim2.new(1, 0, 0, 25)
EventTitle.Position = UDim2.new(0, 0, 0, 10)
EventTitle.Text = "SEA KING / HYDRA"
EventTitle.TextColor3 = HubLib.AccentColor
EventTitle.Font = Enum.Font.GothamBold
EventTitle.TextSize = 11
EventTitle.BackgroundTransparency = 1

local EventTimer = Instance.new("TextLabel", EventFrame)
EventTimer.Size = UDim2.new(1, 0, 0, 20)
EventTimer.Position = UDim2.new(0, 0, 0, 30)
EventTimer.Text = "CONTANDO: --:--"
EventTimer.TextColor3 = Color3.fromRGB(255, 255, 255)
EventTimer.Font = Enum.Font.GothamSemibold
EventTimer.TextSize = 12
EventTimer.BackgroundTransparency = 1

-- ABA 2: COMBAT
local CombatTab = MyHub:CreateTab("Combat")
MyHub:AddToggle(CombatTab, "Auto Buso Haki", function(state)
    _G.AutoBuso = state
    task.spawn(function()
        while _G.AutoBuso do
            if not Players.LocalPlayer.Character:FindFirstChild("Buso") then
                -- Remote ou tecla de ativação aqui
            end
            task.wait(1)
        end
    end)
end)

-- ABA 3: ITEMS & FRUITS (UNIFICADA)
local ItemTab = MyHub:CreateTab("Fruits")
local sBuyKey, bAmt, sUseKey, uType = "Copper Key", 10, "Copper Key", "Open1"

-- SESSÃO FRUTAS BELI
MyHub:AddToggle(ItemTab, "Auto Gacha (Fruta Beli)", function(state)
    _G.AutoGacha = state
    if state then
        task.spawn(function()
            while _G.AutoGacha do
                local remote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Functions"):WaitForChild("CheckBuy")
                remote:InvokeServer("GachaBeli")
                task.wait(5)
            end
        end)
    end
end)

-- SESSÃO CHAVES GACHA
MyHub:AddDropdown(ItemTab, "Comprar Key", {"Copper Key", "Iron Key", "Gold Key"}, function(v) sBuyKey = v end)
MyHub:AddTextBox(ItemTab, "Quantidade Key (1-10)", "10", function(v) bAmt = tonumber(v) or 10 end)
MyHub:AddButton(ItemTab, "BUY SELECTED KEYS", function()
    ReplicatedStorage.Chest.Remotes.Functions.BuyKey:InvokeServer(sBuyKey, bAmt)
end)

MyHub:AddDropdown(ItemTab, "Usar Key", {"Copper Key", "Iron Key", "Gold Key", "Platinum Key", "Diamond Key"}, function(v) sUseKey = v end)
MyHub:AddDropdown(ItemTab, "Modo de Uso", {"Open1", "Open10"}, function(v) uType = v end)
MyHub:AddButton(ItemTab, "USE SELECTED KEYS", function()
    if uType == "Open10" then
        ReplicatedStorage.Chest.Remotes.Functions.UseKey:InvokeServer(sUseKey, "Open10")
    else
        task.spawn(function()
            for i = 1, bAmt do
                ReplicatedStorage.Chest.Remotes.Functions.UseKey:InvokeServer(sUseKey, "Open1")
                task.wait(0.05)
            end
        end)
    end
end)

MyHub:AddToggle(ItemTab, "Material Farmer (Item Search)", function(state) _G.ItemSearch = state end)
MyHub:AddToggle(ItemTab, "Shop Sniper (Rare Fruits)", function(state) _G.ShopSniper = state end)

-- ABA 4: UTILS & CODES
local UtilTab = MyHub:CreateTab("Utils")
MyHub:AddToggle(UtilTab, "Anti-AFK", function(state)
    local virtualUser = game:GetService("VirtualUser")
    Players.LocalPlayer.Idled:Connect(function()
        if _G.AntiAFK then
            virtualUser:CaptureController()
            virtualUser:ClickButton2(Vector2.new())
        end
    end)
end)
MyHub:AddButton(UtilTab, "Auto Redeem All Codes", function() print("Redeeming Codes...") end)
MyHub:AddButton(UtilTab, "Server Hop", function() 
    -- Lógica de Server Hop aqui
end)

-- ABA 5: EQUIPAMENTO
local EquipTab = MyHub:CreateTab("Equip")
HubLib:AddEquipDropdown(EquipTab)

-- LÓGICA DE ATUALIZAÇÃO DOS MONITORES
task.spawn(function()
    while true do
        local lines = {}
        local f1 = workspace:FindFirstChild("AllspawnDF")
        local f2 = workspace:FindFirstChild("AllDroppedFruit")
        if f1 then for _, f in ipairs(f1:GetChildren()) do table.insert(lines, "[SPAWN] " .. f.Name:upper()) end end
        if f2 then for _, f in ipairs(f2:GetChildren()) do table.insert(lines, "[DROP] " .. f.Name:upper()) end end
        FruitList.Text = #lines > 0 and table.concat(lines, "\n") or "NENHUMA FRUTA NO MAPA"

        pcall(function()
            local skLabel = Players.LocalPlayer.PlayerGui.MainGui.StarterFrame.LegacyPoseFrame.SecondSea.SKTimeLabel
            if skLabel then
                EventTimer.Text = skLabel.Text
                EventTitle.Text = string.find(string.lower(skLabel.Text), "hydra") and "HYDRA EVENT" or "SEA KING EVENT"
            end
        end)
        task.wait(1)
    end
end)
