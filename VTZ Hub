-- [[ VTZ HUB - REORGANIZED EDITION ]]

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ==========================================
-- 1. ENGINE DA INTERFACE (BIBLIOTECA)
-- ==========================================
local HubLib = {}
HubLib.MainColor = Color3.fromRGB(20, 20, 20)
HubLib.SidebarColor = Color3.fromRGB(15, 15, 15)
HubLib.AccentColor = Color3.fromRGB(81, 74, 255) 
HubLib.ToggleOffColor = Color3.fromRGB(45, 45, 45)
HubLib.TextColor = Color3.fromRGB(255, 255, 255)

local function makeDraggable(frame, parentFrame)
    local dragging, mousePos, framePos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            mousePos = input.Position
            framePos = parentFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - mousePos
            parentFrame.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
        end
    end)
end

function HubLib:Init(hubTitle, subTitle)
    local targetParent = nil
    pcall(function() targetParent = CoreGui end)
    if not targetParent then targetParent = Players.LocalPlayer:WaitForChild("PlayerGui") end
    if targetParent:FindFirstChild("VxezeHub") then targetParent.VxezeHub:Destroy() end

    local ScreenGui = Instance.new("ScreenGui", targetParent)
    ScreenGui.Name = "VxezeHub"
    ScreenGui.ResetOnSpawn = false

    local ToggleBtn = Instance.new("TextButton", ScreenGui)
    ToggleBtn.Name = "VxezeToggle"
    ToggleBtn.Size = UDim2.new(0, 45, 0, 45)
    ToggleBtn.Position = UDim2.new(0, 10, 0.5, -22)
    ToggleBtn.BackgroundColor3 = self.AccentColor
    ToggleBtn.Text = "VX"
    ToggleBtn.TextColor3 = Color3.new(1,1,1)
    ToggleBtn.Font = Enum.Font.GothamBold
    ToggleBtn.TextSize = 14
    Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 10)
    makeDraggable(ToggleBtn, ToggleBtn)

    local MainFrame = Instance.new("Frame", ScreenGui)
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 480, 0, 320)
    MainFrame.Position = UDim2.new(0.5, -240, 0.5, -160)
    MainFrame.BackgroundColor3 = self.MainColor
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

    ToggleBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

    local Sidebar = Instance.new("Frame", MainFrame)
    Sidebar.Size = UDim2.new(0, 120, 1, -35)
    Sidebar.Position = UDim2.new(0, 0, 0, 35)
    Sidebar.BackgroundColor3 = self.SidebarColor
    local sideLayout = Instance.new("UIListLayout", Sidebar)
    sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    sideLayout.Padding = UDim.new(0, 2)
    Instance.new("UIPadding", Sidebar).PaddingTop = UDim.new(0, 10)

    self.PagesFolder = Instance.new("Frame", MainFrame)
    self.PagesFolder.Size = UDim2.new(1, -130, 1, -45)
    self.PagesFolder.Position = UDim2.new(0, 125, 0, 40)
    self.PagesFolder.BackgroundTransparency = 1

    self.Sidebar = Sidebar
    self.Tabs = {}
    return self
end

function HubLib:CreateTab(name)
    local TabBtn = Instance.new("TextButton", self.Sidebar)
    TabBtn.Size = UDim2.new(0.9, 0, 0, 30)
    TabBtn.BackgroundTransparency = 1
    TabBtn.Text = name:upper()
    TabBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
    TabBtn.Font = Enum.Font.GothamSemibold
    TabBtn.TextSize = 11

    local Page = Instance.new("ScrollingFrame", self.PagesFolder)
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.ScrollBarThickness = 0
    local layout = Instance.new("UIListLayout", Page)
    layout.Padding = UDim.new(0, 6)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

    layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        Page.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 10)
    end)

    TabBtn.MouseButton1Click:Connect(function()
        for _, t in pairs(self.Tabs) do
            t.Page.Visible = false
            t.Btn.TextColor3 = Color3.fromRGB(150, 150, 150)
        end
        Page.Visible = true
        TabBtn.TextColor3 = self.AccentColor
    end)

    table.insert(self.Tabs, {Page = Page, Btn = TabBtn})
    if #self.Tabs == 1 then Page.Visible = true TabBtn.TextColor3 = self.AccentColor end
    return Page
end

function HubLib:AddToggle(page, text, callback)
    local ToggleFrame = Instance.new("Frame", page)
    ToggleFrame.Size = UDim2.new(0.95, 0, 0, 38)
    ToggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", ToggleFrame).CornerRadius = UDim.new(0, 5)

    local Label = Instance.new("TextLabel", ToggleFrame)
    Label.Size = UDim2.new(1, -60, 1, 0)
    Label.Position = UDim2.new(0, 12, 0, 0)
    Label.Text = text
    Label.TextColor3 = self.TextColor
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 11
    Label.TextXAlignment = Enum.TextXAlignment.Left

    local Outer = Instance.new("TextButton", ToggleFrame)
    Outer.Size = UDim2.new(0, 38, 0, 18)
    Outer.Position = UDim2.new(1, -48, 0.5, -9)
    Outer.BackgroundColor3 = self.ToggleOffColor
    Outer.Text = ""
    Instance.new("UICorner", Outer).CornerRadius = UDim.new(1, 0)

    local Inner = Instance.new("Frame", Outer)
    Inner.Size = UDim2.new(0, 14, 0, 14)
    Inner.Position = UDim2.new(0, 2, 0, 2)
    Inner.BackgroundColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", Inner).CornerRadius = UDim.new(1, 0)

    local state = false
    Outer.MouseButton1Click:Connect(function()
        state = not state
        pcall(callback, state)
        local targetPos = state and UDim2.new(1, -16, 0, 2) or UDim2.new(0, 2, 0, 2)
        local targetColor = state and self.AccentColor or self.ToggleOffColor
        TweenService:Create(Outer, TweenInfo.new(0.2), {BackgroundColor3 = targetColor}):Play()
        TweenService:Create(Inner, TweenInfo.new(0.2), {Position = targetPos}):Play()
    end)
end

function HubLib:AddButton(page, text, callback)
    local Btn = Instance.new("TextButton", page)
    Btn.Size = UDim2.new(0.95, 0, 0, 35)
    Btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Btn.Text = text
    Btn.TextColor3 = self.TextColor
    Btn.Font = Enum.Font.GothamSemibold
    Btn.TextSize = 11
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 5)
    Btn.MouseButton1Click:Connect(callback)
end

function HubLib:AddTextBox(page, text, placeholder, callback)
    local TFrame = Instance.new("Frame", page)
    TFrame.Size = UDim2.new(0.95, 0, 0, 45)
    TFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", TFrame)
    
    local Label = Instance.new("TextLabel", TFrame)
    Label.Size = UDim2.new(1, 0, 0, 20)
    Label.Position = UDim2.new(0, 10, 0, 2)
    Label.Text = text
    Label.TextColor3 = Color3.fromRGB(180, 180, 180)
    Label.Font = Enum.Font.Gotham
    Label.TextSize = 10
    Label.BackgroundTransparency = 1
    Label.TextXAlignment = Enum.TextXAlignment.Left

    local Box = Instance.new("TextBox", TFrame)
    Box.Size = UDim2.new(1, -20, 0, 20)
    Box.Position = UDim2.new(0, 10, 0, 20)
    Box.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Box.PlaceholderText = placeholder
    Box.Text = "10"
    Box.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", Box)
    Box.FocusLost:Connect(function() callback(Box.Text) end)
end

function HubLib:AddGridSelector(page, title, options, callback)
    local Container = Instance.new("Frame", page)
    Container.Size = UDim2.new(0.95, 0, 0, 65)
    Container.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Instance.new("UICorner", Container)

    local Label = Instance.new("TextLabel", Container)
    Label.Size = UDim2.new(1, 0, 0, 20)
    Label.Text = title:upper()
    Label.TextColor3 = self.AccentColor
    Label.Font = Enum.Font.GothamBold
    Label.TextSize = 10
    Label.BackgroundTransparency = 1

    local Grid = Instance.new("Frame", Container)
    Grid.Size = UDim2.new(1, -10, 0, 35)
    Grid.Position = UDim2.new(0, 5, 0, 22)
    Grid.BackgroundTransparency = 1
    local layout = Instance.new("UIListLayout", Grid)
    layout.FillDirection = Enum.FillDirection.Horizontal
    layout.Padding = UDim.new(0, 4)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

    local btns = {}
    for _, opt in pairs(options) do
        local b = Instance.new("TextButton", Grid)
        b.Size = UDim2.new(0, 60, 1, 0)
        b.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
        b.Text = opt:gsub(" Key", "")
        b.TextColor3 = Color3.new(0.7,0.7,0.7)
        b.Font = Enum.Font.GothamSemibold
        b.TextSize = 8
        Instance.new("UICorner", b)
        
        b.MouseButton1Click:Connect(function()
            for _, v in pairs(btns) do v.BackgroundColor3 = Color3.fromRGB(40, 40, 40) v.TextColor3 = Color3.new(0.7,0.7,0.7) end
            b.BackgroundColor3 = self.AccentColor
            b.TextColor3 = Color3.new(1,1,1)
            callback(opt)
        end)
        table.insert(btns, b)
    end
end

-- ==========================================
-- 2. INICIALIZAÇÃO E ABAS
-- ==========================================
local MyHub = HubLib:Init("KING LEGACY", "VTZ HUB")
local FruitTab = MyHub:CreateTab("Fruits")
local GachaTab = MyHub:CreateTab("Gacha")

local sBuyKey, bAmt, sUseKey, uType = "Copper Key", 10, "Copper Key", "Open1"

-- [ ABA FRUITS - TP E MONITOR ]
MyHub:AddToggle(FruitTab, "Auto Collect Fruit Map", function(state)
    _G.AutoFruit = state
    if state then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/tiktokbot116-commits/Scripts/refs/heads/main/Auto%20Collect%20Fruit%20Map"))()
    end
end)

MyHub:AddToggle(FruitTab, "Auto Collect All Chests", function(state)
    _G.AutoChest = state
    if state then
        loadstring(game:HttpGet("https://raw.githubusercontent.com/tiktokbot116-commits/Scripts/refs/heads/main/Auto%20Farm%20Chest"))()
    end
end)

local Monitor = Instance.new("Frame", FruitTab)
Monitor.Size = UDim2.new(0.95, 0, 0, 60)
Monitor.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Instance.new("UICorner", Monitor)

local MTitle = Instance.new("TextLabel", Monitor)
MTitle.Size = UDim2.new(1, 0, 0, 20)
MTitle.Text = "MONITOR DE FRUTAS"
MTitle.TextColor3 = HubLib.AccentColor
MTitle.Font = Enum.Font.GothamBold
MTitle.TextSize = 10
MTitle.BackgroundTransparency = 1

local FruitList = Instance.new("TextLabel", Monitor)
FruitList.Size = UDim2.new(1, -20, 0, 30)
FruitList.Position = UDim2.new(0, 10, 0, 22)
FruitList.Text = "SCANNING..."
FruitList.TextColor3 = Color3.fromRGB(200, 200, 200)
FruitList.Font = Enum.Font.Gotham
FruitList.TextSize = 9
FruitList.BackgroundTransparency = 1
FruitList.TextYAlignment = Enum.TextYAlignment.Top

task.spawn(function()
    while true do
        local lines = {}
        local f1 = workspace:FindFirstChild("AllspawnDF")
        local f2 = workspace:FindFirstChild("AllDroppedFruit")
        if f1 then for _, f in ipairs(f1:GetChildren()) do table.insert(lines, "[MAPA] "..f.Name) end end
        if f2 then for _, f in ipairs(f2:GetChildren()) do table.insert(lines, "[DROP] "..f.Name) end end
        FruitList.Text = #lines > 0 and table.concat(lines, " | ") or "NENHUMA FRUTA NO MAPA"
        task.wait(2)
    end
end)

-- [ ABA GACHA - COMPRA E USO DE CHAVES ]
MyHub:AddToggle(GachaTab, "Auto Gacha (Fruta Beli)", function(state)
    _G.AutoGacha = state
    task.spawn(function()
        while _G.AutoGacha do
            pcall(function() ReplicatedStorage.Remotes.Functions.CheckBuy:InvokeServer("GachaBeli") end)
            task.wait(5)
        end
    end)
end)

MyHub:AddGridSelector(GachaTab, "Buy Keys (Escolher qual)", {"Copper Key", "Iron Key", "Gold Key"}, function(v) sBuyKey = v end)
MyHub:AddTextBox(GachaTab, "Quantidade Keys", "10", function(v) bAmt = tonumber(v) or 10 end)
MyHub:AddButton(GachaTab, "EXECUTAR COMPRA DE KEY", function()
    ReplicatedStorage.Chest.Remotes.Functions.BuyKey:InvokeServer(sBuyKey, bAmt)
end)

MyHub:AddGridSelector(GachaTab, "Use Keys (Escolher qual)", {"Copper Key", "Iron Key", "Gold Key", "Platinum Key", "Diamond Key"}, function(v) sUseKey = v end)
MyHub:AddGridSelector(GachaTab, "Modo de Uso", {"Open1", "Open10"}, function(v) uType = v end)
MyHub:AddButton(GachaTab, "EXECUTAR USO DE KEY", function()
    if uType == "Open10" then
        ReplicatedStorage.Chest.Remotes.Functions.UseKey:InvokeServer(sUseKey, "Open10")
    else
        task.spawn(function()
            for i = 1, bAmt do
                ReplicatedStorage.Chest.Remotes.Functions.UseKey:InvokeServer(sUseKey, "Open1")
                task.wait(0.05)
            end
        end)
    end
end)
